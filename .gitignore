
# macOS
.DS_Store
data/**/.DS_Store
data/nki/.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
*.swp

# Jupyter
.ipynb_checkpoints/

# SLURM / LSF job output
*.out
*.err
nohup.out
slurm-*.out
logs/
preprocess/logs/

# Temporary files
tmp/
*.old

# NIfTI data and zips that aren't ROIs committed intentionally
data/rois/mtl-hi/
data/rois/mtl/
data/rois/mtl.zip

# Diagnosis / scratch output
diag_dist/

# rft1d library (external dependency, not ours)
rft1d/

# Probtrackx GPU binary directory
preprocess/bedpostx_gpu/

# v2 route extraction: generated QA figures/HTML (large, regenerable via
# make_route_method_explainer.py / make_routes_3d_html*.py / etc) and the
# per-cohort distance-map cache (regenerable via get_or_compute_dist)
project/route_dijkstra_out/
project/qa_diag/route_v2_dist_cache/
